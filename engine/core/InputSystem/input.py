# engine/core/InputSystem/input_enum.py
from enum import Enum, auto

class Key(Enum):
    UP = auto()
    DOWN = auto()
    LEFT = auto()
    RIGHT = auto()
    ENTER = auto()
    ESCAPE = auto()
    SPACE = auto()
    # Add more keys as needed
